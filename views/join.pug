doctype

html
  head
    meta(charset='UTF-8')
    title = title
    meta(name='viewport' content='width=device-width, user-scalable=no')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    link(rel='stylesheet' href='/join.css')
  body
    div(id = "container")
      div(id = "img-container")
      div(id = "log-container")
        .content-header
          h1 Sign
        form#join-form(action='/auth/join' method= 'post')
          .input-group
            input#join-email(type='email' name='email' placeholder='E-mail')
          .input-group
            input#join-nick(type='text' name='nick' placeholder='Nick')
          .input-group
            input#join-password(type='password' name='password' placeholder='Password')
          if joinError
            .error-message= joinError
          div(class = 'btn-class')
            button#join-btn.btn(type='submit') 회원가입